# =============================================================================
# MathEd Romania — Local Development Environment
# =============================================================================
# Copy this to .env in the project root (or backend/ directory)

# Django
DJANGO_SECRET_KEY=dev-secret-key-change-in-production
DJANGO_SETTINGS_MODULE=config.settings.development

# Database (matches docker-compose.yml defaults)
DB_NAME=mathed_romania
DB_USER=mathed_user
DB_PASSWORD=mathed_local_pass
DB_HOST=localhost
DB_PORT=5432

# Redis
REDIS_URL=redis://localhost:6379/0
